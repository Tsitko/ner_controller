# Infrastructure Layer

External integrations such as GLiNER-backed NER extractors.

Notes:
- GLiNER loading is configured for offline-only use by default via
  `GlinerEntityExtractorConfig.local_files_only`.
