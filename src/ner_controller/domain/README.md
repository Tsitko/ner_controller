# Domain Layer

Business logic and domain entities for hallucination detection and text processing.

Notes:
- Entity deduplication uses normalized Levenshtein similarity (relative threshold, min distance 3).
