# API Layer

FastAPI routers, schemas, and API configuration for HTTP endpoints.

Notes:
- Hallucination, text, and file processing requests prefer `entity_types`; legacy `entities_types` is accepted.
